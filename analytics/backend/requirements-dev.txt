# O núcleo (lab/) roda sem nada disto. Estas dependências existem para os
# testes de conferência, que comparam nossa implementação com referências
# independentes — e para a API.
pytest>=8.0
scipy>=1.11        # confere distribuições e testes
statsmodels>=0.14  # confere Holm e Benjamini-Hochberg
sqlalchemy>=2.0    # persistência (o núcleo estatístico segue sem dependência)
psycopg[binary]>=3.1  # driver PostgreSQL para produção
fastapi>=0.115         # API REST
pydantic[email]>=2.0   # validação de entrada
httpx>=0.27            # cliente de teste da API
uvicorn[standard]>=0.30  # servidor ASGI
openpyxl>=3.1         # só nos testes: confere o XLSX que escrevemos à mão
